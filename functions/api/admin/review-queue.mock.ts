/**
 * Deliberately empty local fixtures.
 *
 * Review data must be sourced from an approved Santa Cruz source record; the
 * repository does not ship inherited municipality examples as fake evidence.
 */
export const mockReviewQueue: unknown[] = [];
export const mockErrors: Record<string, unknown>[] = [];
export const mockConflicts: Record<string, unknown>[] = [];
export const mockSession: null = null;
